## 2025-05-15 - Localized Loading Feedback
**Learning:** For background data refreshes (like cloud sync), global page-blocking overlays (showLoading) are disruptive. Localized feedback (spinning the specific button) keeps the user in context and improves perceived performance.
**Action:** Use `.btn-spin` and button disabling for background refreshes instead of global overlays.
